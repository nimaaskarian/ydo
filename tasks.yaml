better-md:
    task: improve markdown output
    done: true
cli:
    task: complete frictionless cli
    deps: [markdown, fix-minor]
    done: true
    auto-complete: true
core:
    task: complete core
    deps: [due, date-added, file-dep]
    auto-complete: true
date-added:
    task: add task "date added"
due:
    task: add task due
file-dep:
    task: add task file dependency
filedep:
    task: add file dependencies
    deps: [relative]
fix-minor:
    task: fix minor issues and inconveniences
markdown:
    task: fix markdown output
    deps: [tree, markdown-key, better-md, md-circular, sort-date]
    done: true
    auto-complete: true
markdown-key:
    task: add key to markdown output
    done: true
md-circular:
    task: improve fix markdown circular output
    done: true
relative:
    task: relative path todos
sort-date:
    task: sort markdown based on date added instead of depth
    deps: [date-added]
tree:
    task: read tree depth to see which one to start with
    done: true
webgui:
    task: add webgui
ydo:
    task: complete ydo development
    deps: [webgui, filedep, cli, core]
    auto-complete: true
